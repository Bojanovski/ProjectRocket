class ShipSettings {

  constructor () {
    this.layout = []; // array of int arrays, from the bottom to the top, each int represents node type
    this.verticalDistance = 50.0;
    this.horizontalDistance = 50.0;
    this.nodeRadius = 10.0;
  }

}
